# Authentication Service

Auth Service for user authentication and role management in a JS backend. This project includes Sequelize models, controllers, services, routes, validation middleware, and database migrations and seeders.

## Run
Use `npm start` to run the service locally.

### Model Commands
```bash
npx sequelize model:generate --name User --attributes email:string,password:string
```
